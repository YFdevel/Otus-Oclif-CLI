version: 2
updates:
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'weekly'
      day: 'saturday'
    versioning-strategy: 'increase'
    labels:
      - 'dependencies'
    open-pull-requests-limit: 5
    pull-request-branch-name:
      separator: '-'
    commit-message:
      # cause a release for non-dev.js-deps
      prefix: fix(deps)
      # no release for dev.js-deps
      prefix-development: chore(dev.js-deps)
    ignore:
      - dependency-name: '@salesforce/dev.js-scripts'
      - dependency-name: '*'
        update-types: ['version-update:semver-major']
